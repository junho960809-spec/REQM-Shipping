# 변경 기록

## 1.0.27

- 이알아이 주문 파일에 `수령인전화번호`와 `수령인핸드폰번호`가 함께 있을 때 실제 휴대폰번호 열을 우선 사용
- 값이 `--`인 수령인 전화번호 열 때문에 출고 연락처가 비어 있던 문제 수정

## 1.0.26

- 창고이동 입력 화면을 날짜, 사용자, 인증키, 창고, 적요의 5개 행으로 축소
- 사용자 ID와 담당자코드를 `JUNHO191 / 00210` 형식으로 한 칸에 표시
- 출발창고와 도착창고를 `100 → 300` 형식의 한 행으로 통합
- 판매처 선택을 제거하고 현재 파일의 전체 주문을 자동 집계하며 판매처 정보는 적요에 자동 추가
- ID별 API 인증키를 Windows DPAPI로 암호화 저장하고 사용자 선택 시 자동 불러오기
- 인증키는 항상 마스킹하며 관리자 전용 창고이동 화면에서만 저장·삭제 가능

## 1.0.25

- 회사코드 `304293`과 ZONE `AB`를 창고이동 화면의 고정값으로 적용
- 이카운트 사용자 ID·담당자코드·표시이름을 등록, 수정, 삭제하는 관리 팝업 추가
- 저장된 사용자를 선택하거나 등록 ID를 직접 입력하면 담당자코드 자동 입력
- 사용자 프로필을 Windows 사용자별 로컬 데이터 폴더에 저장해 업데이트 후에도 유지

## 1.0.24

- 최초 발급 테스트 인증키용 `sboapi`와 검증 완료 운영키용 `oapi` 모드 선택 추가
- ZONE 조회, 로그인, 창고이동 요청을 선택한 인증키 종류에 맞는 호스트로 일관되게 전송
- 이카운트 요청 헤더에 `Accept: application/json`을 명시해 API 호환성 강화

## 1.0.23

- 이카운트 로그인 세션을 창고이동 URL에 안전하게 인코딩해 인증값 손실 방지
- 로그인 실패와 창고이동 API 권한 실패를 구분해 안내하도록 오류 메시지 개선
- 인증키 발급 사용자, 창고이동 입력 권한, Open API 사용 권한 확인 절차 표시

## 1.0.22

- 관리자 전용 이카운트 창고이동 기능 추가
- 판매처별 출고 주문을 선택하고 세트 구성 수량까지 품목코드별 자동 집계
- 정확·별칭·수동확정 항목만 이동하고 확인 필요·미등록·중복 항목 자동 제외
- API 인증키를 파일에 저장하지 않고 실행 시에만 사용하는 보안 구조 적용
- 전송 전 품목·수량 미리보기와 2단계 확인, 전표번호 결과 표시 추가
- API 호출 중 화면 멈춤 방지를 위한 백그라운드 처리와 실행 중 종료 차단
- 인증정보 없이 요청 해시만 로컬에 기록해 앱 재실행 후에도 동일 이동 요청의 중복 전송 차단

## 1.0.21

- 주문 파일의 `수령인 전화번호` 헤더를 연락처 열로 자동 인식
- 전화번호·우편번호·주소 등 핵심 출고 열의 별칭을 찾지 못하면 사용자 매핑 팝업 표시
- 사용자가 연결한 입력 양식을 저장해 같은 형식의 다음 파일부터 자동 적용

## 1.0.20

- 입력 파일 유형과 변환 출력 양식을 분리해 사용자가 원하는 출력 양식을 선택하도록 개선
- `기본 B2C 양식`, `B2C 사입형 출고건` 기본 출력 양식 제공
- Excel 출력 양식 등록, 열 연결, 선택, 삭제 및 다음 실행 재사용 기능 추가
- 선택한 출력 양식의 헤더·서식을 유지하면서 분석된 출고 데이터를 지정 열에 입력

## 1.0.19

- 위킵 `B2C 사입형 출고건` 양식 자동 판별 및 기본 B2C 출고파일 변환 지원
- 색칠된 필수값 `상품명(C)`, `수량(E)`, `수령자(H)`, `핸드폰(J)`, `우편번호(K)`, `주소(L)` 누락 시 원본 행 안내
- 주문번호가 없는 사입형 주문도 정상 분석하도록 전용 필드 규칙 적용
- Excel 입력 파일 분석 후 파일 잠금이 남지 않도록 읽기 자원 정리

## 판매전표 반자동화 개선

- 이카운트가 거래처코드만으로 인식하도록 결과 Excel의 `거래처명` 열은 유지하고 값은 공란 처리
- 본사출고 조회창에서 선택 품목을 본사출고 대상에서 해제하고 기본창고로 되돌리는 기능 추가
- 최종 Excel에서 본사창고 100 출고 품목을 위킵창고 300 품목보다 위쪽에 모아 출력
- 스마트스토어 `배송비 합계`에 이미 포함된 제주·도서 추가배송비를 다시 더하지 않도록 이중 계상 수정
- 사용자가 지정한 본사출고 품목을 창고 100으로 분리하고, 별도 조회창에서 해당 품목만 Excel 저장
- 자동 변환·품목 집계·확인 필요·배송비 검수·DB 관리 표에 열 선택 필터 추가
- 품목을 Supabase에서는 유지하면서 현재 프로그램 DB에서만 삭제하고 별도 삭제 품목 화면에서 복구하는 기능 추가
- 사용 빈도가 낮은 `DB 요약(피벗)` 화면 제거
- 프로그램 안에서 기준 품목, 상품 매칭, 세트 구성, 배분 가격을 조회·검색·등록·수정하는 `DB 관리` 탭 추가
- 품목코드·품목명 및 상품명·옵션·총금액 중복 등록 차단
- DB 표의 열 제목 클릭 정렬과 품목별 매칭 수·가격 규칙 수·가격 범위를 보여주는 DB 요약 추가
- 변환 결과를 품목별 수량·평균 단가·합계 금액으로 집계하는 `품목 집계` 탭 추가
- `위킵_이카운트_양식 추가.xlsx`에서 검수한 신규 상품 매핑과 세트 가격 규칙 반영
- 행사·공동구매 상품처럼 세트 분류지만 실제 구성품이 1개인 경우에도 DB 저장과 자동 변환 허용
- 여러 결제일이 포함된 원본 파일의 시작일·종료일을 자동 인식하고 전체 기간 합산
- 구매확정 파일을 추가 입력해 품목·수량·상품금액·배송비까지 원본과 통합
- 원본과 구매확정 파일 사이 상품주문번호 중복 시 이중계상 방지를 위해 분석 차단
- 세트 DB 매칭 품목 선택창에 코드·품목명 부분 검색 자동완성 추가
- 자동 변환 결과에 품목코드·품목명 실시간 필터 추가
- 스마트스토어 `최종 상품별 총 주문금액` 우선 적용 및 금액 차이 발생 시 저장 차단
- 배송비 묶음번호별 중복 제거와 `배송비 합계 + 추가배송비` 기준 배송비 집계
- 배송비 금액별 수량 행 생성 및 할인·추가배송비 조정건 시각화
- 확인 필요 항목의 DB 단품 즉시 추가와 이번 분석 제외 기능 추가
- 원화 금액의 소수점 제거, 메인 창 축소 및 스크롤바 확대

## 1.0.18

- 로그인과 DB 새로고침에 중복 구현돼 있던 Supabase 전체 조회 코드를 공통 함수로 통합
- Supabase 기본 조회 한도를 넘는 데이터도 누락 없이 페이지 단위로 불러오도록 단일 처리 경로 적용
- DB 조회 코드 중복 제거로 유지보수성과 데이터 로딩 안정성 개선

## 1.0.17

- 드래그 앤 드롭 영역을 92×92로 추가 축소
- 로그인 입력 영역과 파일 입력 카드를 같은 줄에 배치하고 파일 입력을 오른쪽 정렬
- 면세점 출고지 카드를 분석 결과 제목과 같은 줄의 오른쪽에 배치
- 상단 여백과 카드 내부 간격을 줄여 분석 결과 표의 사용 높이 확대

## 1.0.16

- REQM 공식 브랜드 스토어의 검은 로고 타이포와 넓은 흰 여백을 프로그램 디자인에 반영
- 제품 카드 느낌의 밝은 회색·큰 둥근 모서리 카드 디자인 적용
- 공식 홈페이지 여름 이미지에서 가져온 민트·청록과 모래색을 상태·파일·출고지 포인트로 적용
- 로그인·파일 선택·출고 변환 주요 버튼을 REQM 로고와 같은 검은색으로 통일
- 표와 스크롤바를 브랜드 팔레트에 맞춰 밝고 단정한 스타일로 변경

## 1.0.15

- 파일 선택 버튼을 드래그 앤 드롭 영역 위로 이동하고 글자 크기에 맞게 축소
- 드래그 앤 드롭 영역을 중앙 정사각형 형태로 축소
- 로그인 입력 카드와 이메일·비밀번호 입력칸 너비 축소
- 면세점 출고지 선택창을 대표 출고지명이 보이는 길이로 축소하고 중복 채널명 제거
- 버전을 제목 오른쪽 아래의 작은 글자로 표시하고 별도 테두리 없이 정리

## 1.0.14

- 검증되지 않은 이카운트 창고이동 기능과 화면 버튼 제거
- 창고이동 API, 담당자·창고 조회, API 인증키 저장 및 전용 품목 기준표 코드 제거
- 기존 PC에 남아 있는 창고이동 API 인증키 파일을 업데이트 후 자동 삭제
- 로그인 시 불필요한 담당자·창고 테이블 조회를 제거해 초기 데이터 로딩 최적화

## 1.0.13

- 이카운트 API 직접실행과 동일한 `sboapi` 서버를 창고이동 설정에서 선택 가능
- 로그인과 창고이동 등록이 반드시 같은 API 서버를 사용하도록 통일
- 운영 `oapi` 서버 선택 기능을 유지하고 직접실행 호환 서버를 기본값으로 적용
- `SESSION_ID` URL 인코딩 및 `Accept`, `Content-Type`, `User-Agent` 요청 헤더 보완

## 1.0.12

- 이카운트 창고이동 API가 오류 응답을 반환할 때 `성공 0건 / 실패 0건`으로만 표시하던 문제 수정
- 중첩된 `Data`, `Error`, `Errors`, `Message`, `ResultDetails` 응답을 분석해 실제 이카운트 오류 문구 표시
- 이카운트 응답 형식이 예상과 다를 때 상태와 원본 응답을 표시해 등록 실패 원인을 즉시 확인 가능

## 1.0.11

- 이카운트 재고현황에서 받은 품목코드·품명 기준표 169개를 창고이동 전용 기준으로 내장
- 창고이동 입력 품목명을 이카운트 품명과 정확·유사 대조하여 실제 이카운트 품목코드 자동 적용
- `QP1000C`와 `QP1000C1`처럼 판매처·이카운트 간 모델 표기가 조금 다른 경우에도 제품 종류와 색상을 함께 비교
- Supabase 출고용 품목코드와 이카운트 창고이동용 품목코드를 분리해 최종 전송 코드 정확성 개선
- 실제 등록 확인창에서 최종 이카운트 코드·품명·수량을 표시

## 1.0.10

- 창고이동 등록 직전에 품목코드가 비어 있거나 DB에 없는 경우 품목명으로 표준 품목코드 자동 보완
- 세트상품 품목명은 DB 구성품 코드와 수량으로 자동 분해
- 품목명 후보가 여러 개이거나 DB에서 찾지 못하면 실제 API 전송을 차단하고 해당 행 안내
- 등록된 품목코드는 DB의 표준 품목명과 함께 이카운트 창고이동 요청에 사용

## 1.0.9

- 트래블메이트 매장 발주서 전용 구조 자동 인식 추가
- 상단 사업자 정보가 아니라 `TM 상품코드`, `TM 상품명`, `발주수량`, `매장명` 표를 출고 데이터로 분석
- 발주수량이 입력된 품목만 가져오고 판매처 상품코드와 품목명을 DB에 순차 대조
- 하단 매장 정보에서 출고 주소, 담당 매니저 및 연락처를 자동 추출
- 파일 내 출고지가 확인되면 별도의 출고지 선택 없이 매장 출고용 변환 활성화

## 1.0.8

- 출고 작업 파일 선택 버튼을 유지하면서 Excel·CSV·PDF 드래그 앤 드롭 영역 추가
- 텍스트·표 기반 PDF에서 주문번호, 수령인, 품목명, 품목코드, 수량, 주소, 연락처 등 출고 열 자동 탐지
- 파일의 품목코드를 DB 품목코드와 우선 대조하고 원본 품목코드를 분석 결과 표에 표시
- PDF에 주문번호 또는 수령인이 누락되면 오류 항목으로 표시하고 잘못된 출고 변환 방지
- 이미지로만 구성된 PDF는 OCR 필요 여부를 안내하도록 오류 메시지 개선

## 1.0.7

- 이카운트 창고이동 담당자를 코드뿐 아니라 이름으로도 인식해 담당자 코드로 변환
- 보내는 창고와 받는 창고를 코드뿐 아니라 창고명으로도 인식해 창고 코드로 변환
- 띄어쓰기와 영문 코드 대소문자 차이를 허용하고, 중복 이름은 목록 선택을 요구하도록 안전성 개선
- 등록되지 않은 이름을 API 코드로 그대로 전송하지 않도록 검증 강화

## 1.0.6

- 이카운트 API 인증키를 Windows DPAPI로 현재 PC·Windows 사용자에 암호화 저장
- 일반 사용자는 저장 여부와 마스킹 값만 확인하고 인증키 원문을 표시·수정할 수 없도록 제한
- 관리자만 인증키 표시, 변경, 암호화 저장 및 저장값 삭제 가능
- API 인증키 원문이 설정 파일·Supabase·GitHub·로그에 기록되지 않도록 분리

## 1.0.5

- 기존 B2C/B2B, 판매처 직접양식, 출고지, DB 관리 및 자동 업데이트 기능과 1.0.4 이카운트 창고이동 보드 통합
- 업데이트 시 실행 파일 잠금 재시도, 교체 파일 해시 검증 및 PyInstaller 재실행 환경 초기화 적용
- PDF 분석, 변환 참고파일 및 Windows VC 런타임을 함께 포함하는 통합 PyInstaller 빌드 설정 추가
- 소스에 입력되어 있던 기본 로그인 이메일 제거

## 1.0.4

- 이카운트 창고이동 보드 추가
- 담당자 및 창고 Supabase 조회와 코드 직접 입력 추가
- 달력 방식 일자 입력 추가
- Excel/PDF 드래그 앤 드롭과 파일 선택 추가
- 품목명/품목코드 분석 및 수동 수정 추가
- 이카운트 `SaveLocationTran` 로그인·등록·결과 확인 추가
- API 정보 입력을 우측 상단 팝업으로 이동
- PDF 빌드 의존성과 휴대 가능한 PyInstaller spec 추가
- 프로젝트 인수인계 문서 추가

## 기존 주요 기능

- 셀메이트 B2C 및 면세점 B2B 파일 처리
- 입력 양식 자동 판별
- Supabase 기반 단품/세트상품 매칭
- 유사·확인필요·미등록 표시 및 수동 수정
- 관리자 전용 품목 DB 관리
- 합포장과 중복 출고 구분
- 택배 출고용 Excel 생성
- AI 채팅 기능 제거
